diff --git a/dwm.c b/dwm.c
index 1e96a9d..32c82ad 100644
--- a/dwm.c
+++ b/dwm.c
@@ -754,7 +754,12 @@ drawbar(Monitor *m)
 	if ((w = m->ww - tw - x) > bh) {
 		if (m->sel) {
 			drw_setscheme(drw, scheme[SchemeNorm]);
-			drw_text(drw, x, 0, w, bh, lrpad / 2, m->sel->name, 0);
+			// title is bigger than the width of the title rectangle, don't center
+ 			if (TEXTW(m->sel->name) > w)
+ 				drw_text(drw, x, 0, w, bh, lrpad / 2, m->sel->name, 0);
+			// center window title
+ 			else /* center window title */
+ 				drw_text(drw, x, 0, w, bh, (w - TEXTW(m->sel->name)) / 2, m->sel->name, 0);
 			if (m->sel->isfloating)
 				drw_rect(drw, x + boxs, boxs, boxw, boxw, m->sel->isfixed, 0);
 		} else {
