#!/bin/sh

THIS_HOME_DIR=/home/chuck

# symlink all user bin to user directory in path
${THIS_HOME_DIR}/.local/bin/tools/utils/symlinks/refresh-user-bin-links

# symlink resources to root folder
doas refresh-root-links

