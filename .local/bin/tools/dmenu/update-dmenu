#!/bin/sh

THIS_DEPENDENCY_CATEGORY=x11-misc
THIS_PORTAGE_SAVEDCONFIG_DIR=/etc/portage/savedconfig

cp $(find "${THIS_PORTAGE_SAVEDCONFIG_DIR}/${THIS_DEPENDENCY_CATEGORY}/src/" -maxdepth 1 -type l) "${THIS_PORTAGE_SAVEDCONFIG_DIR}/${THIS_DEPENDENCY_CATEGORY}/"
emerge --ask=n x11-misc/dmenu
