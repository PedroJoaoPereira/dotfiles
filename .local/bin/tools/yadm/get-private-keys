#!/bin/bash

if [ ! -d ~/.config/dotfiles-secrets ]; then
	cd ~/.config
	read -p "PedroJoaoPereira/dotfiles-secrets API access token: " apitoken
	git clone https://$apitoken@github.com/PedroJoaoPereira/dotfiles-secrets.git
fi

cd ~/.config/dotfiles-secrets
git fetch origin
git pull
