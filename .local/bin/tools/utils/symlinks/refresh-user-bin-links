#!/bin/bash

# removes symlinks from ~/.local/bin
find ${HOME}/.local/bin -maxdepth 1 -type l -delete
# creates symlinks in ~/.local/bin
find ${HOME}/.local/bin/tools/ -type f -exec ln -sfv "{}" ${HOME}/.local/bin/ ';'
